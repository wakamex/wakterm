# `ActivateCopyMode`

Activates copy mode!

```lua
config.keys = {
  { key = 'X', mods = 'CTRL', action = wakterm.action.ActivateCopyMode },
}
```

[Learn more about copy mode](../../../copymode.md)

