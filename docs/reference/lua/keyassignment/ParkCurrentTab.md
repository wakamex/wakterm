# `ParkCurrentTab`

Hides the current tab from the normal tab strip while leaving its panes, processes, and agent sessions running. Hidden tabs remain available from `ShowTabNavigator` and survive mux restart.

The default shortcut is `Ctrl-Shift-S`.

Hiding the last visible tab in a window is rejected.
